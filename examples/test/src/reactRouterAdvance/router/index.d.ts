/// <reference types="react" />
import { ExtendedRouterProps } from './types';
export declare const ExtendedRouter: ({ path, component: Component, redirectUrl, guards, resolvers, debounceWaitTime, childs, redirectToChild, exact, location, }: ExtendedRouterProps) => JSX.Element | null;
