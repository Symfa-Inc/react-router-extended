context('Routing', () => {
  // beforeEach(() => {
  //   cy.visit('http://localhost:3000/page-with-redirect');
  // });
  //
  // describe('Redirect page', () => {
  //   it('Redirect page at room, should redirect to home page', () => {
  //     cy.wait(300);
  //     cy.location('pathname').should('eq', '/');
  //
  //     cy.get('#main-page').should('have.text', 'Main page');
  //   });
  // });
});
